<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Entitlements for the bundled CPython and everything it loads. Kept
  separate from the Electron entitlements so that the sidecar's needs
  never silently widen the app's.

  Each key was added only after a hardened-runtime build failed without
  it. See desktop/README.md for the failure each one fixes.
-->
<plist version="1.0">
  <dict>
  </dict>
</plist>
